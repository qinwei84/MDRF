# WHU-Stereo Split

This directory contains the WHU-Stereo official split used in this project.

Files:

```text
train.txt
val.txt
test.txt
```

Each line stores three dataset-relative paths:

```text
left_image right_image disparity_map
```
