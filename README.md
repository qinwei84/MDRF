# Model Weights

This directory contains the curated model weights released with the source package. 

Layout:

```text
weights/hmsmnet/     # HMSMNet host baseline and MDRF variants
weights/psmnet/      # PSMNet transfer baseline and MDRF variants
```